# Función serverless api/chat.js (Vercel) — tu API key de https://console.anthropic.com
ANTHROPIC_API_KEY=

# Solo si el sitio estático (GitHub Pages) y la función viven en dominios distintos:
# URL absoluta de tu función desplegada, ej. https://tu-proyecto.vercel.app/api/chat
# Si el frontend y la función se sirven desde el mismo dominio, deja esto vacío.
VITE_CHAT_API_URL=
